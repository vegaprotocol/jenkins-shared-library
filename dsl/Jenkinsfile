pipeline {
    agent any
    options {
        timestamps()
        ansiColor('x-term')
    }
    stages {
        stage('Create jobs') {
            steps {
                jobDsl(
                    targets: [
                        'dsl/scripts/*.groovy'
                    ].join("\n")
                )
            }
        }
    }
}
